{% extends 'base.html.twig' %}

{% block title %}Barang{% endblock %}

{% block body %}
<div class="text-center"><h2>Edit Data</h2></div>
<div class="container">
    <div class="row">
    {{ include('barang/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('app_barang_index') }}" class="btn btn-sm btn-secondary me-2">back to list</a>
    
    <div class="text-center">
    {{ include('barang/_delete_form.html.twig') }}
    </div>
    
</div>
{% endblock %}

