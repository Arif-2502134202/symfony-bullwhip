{% extends 'base.html.twig' %}

{% block title %}Barang{% endblock %}

{% block body %}
    <h1>Barang</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barang.id }}</td>
            </tr>
            <tr>
                <th>Nama</th>
                <td>{{ barang.nama }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_barang_index') }}" class="btn btn-sm btn-secondary me-2">back to list</a>
    
    <div class="text-center">
    <a href="{{ path('app_barang_edit', {'id': barang.id}) }}" class="btn btn-sm btn-success me-2">edit</a>
    </div>
    {{ include('barang/_delete_form.html.twig') }}
{% endblock %}
