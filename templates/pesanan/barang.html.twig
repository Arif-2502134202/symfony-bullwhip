{% extends 'base.html.twig' %}

{% block title %}Barang{% endblock %}

{% block body %}
<div class="text-center"><h2>Tambah Data</h2></div>
<div class="container">
    <div class="row">
    {{ include('pesanan/_form.html.twig') }}
    </div>
</div>
    <a href="{{ path('app_pesanan_index') }}" class="btn btn-secondary me-2 btn-sm">back to list</a>
{% endblock %}
