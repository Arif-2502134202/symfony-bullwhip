{% extends 'base.html.twig' %}

{% block title %}Edit Pesanan{% endblock %}

{% block body %}
<div class="text-center"><h2>Edit Data</h2></div>
<div class="container">
    <div class="row">
        {{ include('pesanan/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <a href="{{ path('app_pesanan_index') }}" class="btn btn-secondary btn-sm">Kembali</a>
    
    <div class="text-center">
    {{ include('pesanan/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
