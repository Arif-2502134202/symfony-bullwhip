<form method="post" action="{{ path('app_produksi_delete', {'id': produksi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produksi.id) }}">
    <button class="btn">Delete</button>
</form>
