{% extends 'base.html.twig' %}

{% block title %}Produksi{% endblock %}

{% block body %}
    <h1>Produksi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produksi.id }}</td>
            </tr>
            <tr>
                <th>Jumlah</th>
                <td>{{ produksi.jumlah }}</td>
            </tr>
            <tr>
                <th>Leadtime</th>
                <td>{{ produksi.leadtime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produksi_index') }}">back to list</a>

    <a href="{{ path('app_produksi_edit', {'id': produksi.id}) }}">edit</a>

    {{ include('produksi/_delete_form.html.twig') }}
{% endblock %}
